#!/usr/bin/perl
#这是一个演示基本数据类型的程序。

use v5.14;
say "Hello, Perl!";

my $int = 42;
my $pi = 3.1415926;
my $pet = "Camel";

my $radius = 10.1;
my $area = $radius * $radius * $pi;

say "My area is $area.";
